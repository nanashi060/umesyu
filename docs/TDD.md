# TDD

Use failing test, minimal implementation, refactor, docs update.

Required unit test areas:

- Date and reminder logic
- Review request policy
- Amazon search URL generation
- i18n resource shape
- Seed coverage
- Mapper and migration behavior when changed

`npm run quality` is the completion gate.

